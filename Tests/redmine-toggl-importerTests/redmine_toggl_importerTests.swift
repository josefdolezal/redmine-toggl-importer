import XCTest
@testable import redmine_toggl_importer

class redmine_toggl_importerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        XCTAssertEqual(redmine_toggl_importer().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
